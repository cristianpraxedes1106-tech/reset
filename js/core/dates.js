export const today=()=>new Date().toLocaleDateString('en-CA');export const parse=k=>typeof k==='string'&&/^\d{4}-\d\d-\d\d$/.test(k)?new Date(`${k}T12:00:00`):null;export const add=(k,n)=>{const d=parse(k);if(!d)return null;d.setDate(d.getDate()+n);return d.toLocaleDateString('en-CA')};export const pretty=k=>{const d=parse(k);return d?new Intl.DateTimeFormat('pt-BR',{day:'numeric',month:'long'}).format(d):'data indisponível'};export const month=()=>{const d=new Date(),f=new Date(d.getFullYear(),d.getMonth(),1),l=new Date(d.getFullYear(),d.getMonth()+1,0);return{y:d.getFullYear(),m:d.getMonth(),start:(f.getDay()+6)%7,count:l.getDate(),label:new Intl.DateTimeFormat('pt-BR',{month:'long',year:'numeric'}).format(f)}};
